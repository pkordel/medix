default:
  models:
    Team: read
    Membership:
      - read
      - search

editor:
  models:

admin:
  includes:
    - editor
  manageable_roles:
    - admin
    - editor
  models:
    Team: manage
    Membership: manage
