default:
  models:
    Clinic: read
    Membership:
      - read
      - search

editor:
  models:

admin:
  includes:
    - editor
  manageable_roles:
    - admin
    - editor
  models:
    Clinic: manage
    Membership: manage
